# Probability Mass Distribution

## Definition

A **probability mass distribution** (PMD), formally described by a **probability mass function** (PMF), is a function that assigns a probability to each possible outcome of a discrete random variable. For a random variable *X*, the PMF is written as P(X = k), read as "the probability that X takes the value k."

It must satisfy two axioms:
1. **Non-negativity:** P(X = k) ≥ 0 for all k
2. **Normalization:** Σ P(X = k) = 1 over all possible k

---

## Example: Pizza Topping Preference

A survey of 10 individuals records the pizza topping each person prefers. Let *X* be the topping chosen by a randomly selected individual.

| Outcome (k) | Frequency | Relative Frequency | P(X = k) |
|---|---|---|---|
| Cheese | 4 | 4/10 | 0.40 |
| Pepperoni | 3 | 3/10 | 0.30 |
| Mushroom | 2 | 2/10 | 0.20 |
| Olives | 1 | 1/10 | 0.10 |
| **Total** | **10** | **10/10** | **1.00** |

---

## Interpretation of Each Column

**Outcome (k):** The sample space — the exhaustive set of all possible values *X* can take. Any value outside this set has P(X = k) = 0. For instance, P(X = "Anchovies") = 0, as it was not observed.

**Frequency:** The raw count of occurrences for each outcome in the observed data. This is empirical evidence from which probabilities are estimated.

**Relative Frequency:** The proportion of total observations belonging to each outcome (frequency ÷ n). This serves as the empirical estimate of the true underlying probability.

**P(X = k):** The probability mass assigned to each outcome. It quantifies how much of the total probability (1.0) is allocated to that specific value. Outcomes observed more frequently carry greater mass.

---
---
